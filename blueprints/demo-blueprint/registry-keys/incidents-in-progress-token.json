{
  "label": "Incidents in progress token",
  "description": "Token for getting from Tymly front facing API",
  "schema": {
    "properties": {
      "environmentVariableName": "INCIDENTS_IN_PROGRESS_TOKEN",
      "value": {
        "type": "string"
      }
    },
    "required": ["value"]
  }
}