{
  "title": "streetStateCode",
  "description": "Identifies at which point the street record is within its lifecycle",
  "primaryKey": ["value"],
  "type": "object",
  "properties": {
    "value": {
      "type": "integer",
      "maxLength": 2
    },
    "description": {
      "type": "string",
      "maxLength": 20
    }
  },
  "required": ["value", "description"]
}

