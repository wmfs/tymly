'use strict'

module.exports = {
  description: 'Adds user-management capabilities to Flobot (e.g. authorization checks, what flows/forms a user is entitled to instigate etc.)',
  example: require('./example.json')
}
