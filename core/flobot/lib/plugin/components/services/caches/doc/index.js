'use strict'

module.exports = {
  description: 'Adds [lru-cache](https://www.npmjs.com/package/lru-cache)-based caching to Flobot. Note defaults will be used if no `caches` configuration is provided.',
  example: require('./example.json')
}
