{
  "name": "package-with-dependencies",
  "version": "0.0.0",
  "description": "do nothing package with a node_modules directory we don't want to recurse into",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "repository": {
    "type": "fake",
    "url": "fakey://fakey"
  },
  "devDependencies": {
    "mocha": "5.1.0"
  },
  "dependencies": {
    "rillet": "^1.11.0"
  }
}
