{
  "type": "object",
  "properties": {
    "variableName": {
      "type": "string",
      "description": "Name of an environment variable to read a value from"

    },
    "failIfNotSet": {
      "type": "boolean",
      "description": "Should things fail if the environment variable hasn't been set? Defaults to `false`."
    }

  },
  "required": ["variableName"]
}
