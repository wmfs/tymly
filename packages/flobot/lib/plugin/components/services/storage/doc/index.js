'use strict'

module.exports = {
  description: 'This is the default in-memory storage service that ships with Flobot. Useful for testing and not much else.',
  blueprintDirs: {
    models: "One JSON file per model (contents to be a JSON schema for defining the model's data structure)"
  }
}
