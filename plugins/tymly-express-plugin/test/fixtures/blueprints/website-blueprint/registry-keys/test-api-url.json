{
  "label": "Template URL",
  "description": "URL for getting from Tymly front facing API",
  "schema": {
    "properties": {
      "environmentVariableName": "TEST_API_URL",
      "value": {
        "type": "string",
        "default": "Please insert URL here"
      }
    },
    "required": ["value"]
  }
}