{
  "label": "Template URL",
  "description": "URL for getting from Tymly front facing API",
  "schema": {
    "properties": {
      "value": {
        "type": "string",
        "default": "Please insert URL here"
      }
    },
    "required": ["value"]
  }
}