'use strict'

module.exports = {
  description: 'Adds Solr support',
  blueprintDirs: {
    'search-docs': 'Each JSON file is used to translates a model document into standard properties for searching.'
  }
}
