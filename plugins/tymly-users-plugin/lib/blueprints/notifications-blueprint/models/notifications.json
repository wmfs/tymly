{
  "id": "notifications",
  "title": "Notifications",
  "description": "Notifications",
  "primaryKey": ["notificationId"],
  "type": "object",
  "properties": {
    "userId": {
      "type": "string",
      "description": "The ID of the user the notification belongs to"
    },
    "notificationId": {
      "type": "string",
      "description": "A UUID identifying this particular notification for this particular user"
    },
    "title": {
      "type": "string",
      "description": "A short title describing this notification"
    },
    "description": {
      "type": "string",
      "description": "A bit more context/information"
    },
    "category": {
      "type": "string",
      "description": "The category the notification belongs to"
    },
    "launches": {
      "type": "array",
      "description": "Links each notification to an action to perform (i.e. which state machine to execute along with the inputs to provide)",
      "items": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string",
            "description": "Where was the launch triggered?"
          },
          "stateMachineName": {
            "type": "string",
            "description": "What state machine is this part of?"
          },
          "input": {
            "type": "object",
            "properties": {
              "claimNo": {
                "type": "integer",
                "description": "The claim number"
              }
            }
          }
        },
        "required": ["title"]
      }
    }
  },
  "required": ["userID", "notificationId"]
}