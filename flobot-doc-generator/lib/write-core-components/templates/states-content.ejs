## State config
<% if (config) { %>
```json
<%- JSON.stringify(doc.example, null, 2) %>
```
<%= config %>
<% } else {%>
- This state doesn't require any configuration.
<% }%>

<% if (autoNudge) { %>
`autoNudge`: `true`

> This state will automatically advance the flow and doesn't require any external updates
<% } else {%>
`autoNudge`: `false`

> __Note:__ This state will require an external update before flow will advance
<% }%>

